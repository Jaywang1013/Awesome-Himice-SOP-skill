# Local default profiles may contain customer and employee personal information.
**/department-defaults.local.md
*.local.json
*.local.yaml
*.local.yml

# Credentials and local runtime state.
.env
.env.*
!.env.example
.task-runs/

# DSH runtime artifacts & uploaded customer files — never commit.
.dsh-uploads/
.dsh-vision-router/

# Customer-generated files at repo root only (templates under */assets/ stay tracked).
/*.xlsx
/*.xls
/*.pdf
/*.docx
/*.pptx
/*.png
/*.jpg
/*.jpeg

# Generated project outputs and local inspection artifacts.
outputs/
*.inspect.ndjson

# Local upstream clones used for adaptation and evaluation.
.sources/
